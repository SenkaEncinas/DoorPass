import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Fondos principales
  static const Color background = Color(0xFF100018);
  static const Color appBar = Color(0xFF2D014F);

  // Cards
  static const Color card = Color(0xFF2D014F);
  static const Color comboCard = Color(0xFF3A0055);

  // Acentos
  static const Color primaryAccent = Color(0xFF7C4DFF);
  static const Color secondaryAccent = Color(0xFFE040FB);

  // Textos
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.purpleAccent;
  static const Color textMuted = Colors.white70;

  // Otros
  static const Color progress = Colors.purpleAccent;
}
